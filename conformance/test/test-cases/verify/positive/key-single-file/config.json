{
  "description": "Key verification when the model is a single file, not a directory",
  "method": "key",
  "model": "models/single-file",
  "expected_signed_files": [
    "model.bin"
  ],
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem"
  }
}
