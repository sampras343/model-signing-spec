{
  "description": "Go-signed certificate-based verification (cross-client interop: Go signs, test client verifies)",
  "method": "certificate",
  "model": "models/simple",
  "expected_signed_files": ["signme-1", "signme-2"],
  "verify": {
    "cert_chain": [
      "keys/certificate/ca-cert.pem"
    ],
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
