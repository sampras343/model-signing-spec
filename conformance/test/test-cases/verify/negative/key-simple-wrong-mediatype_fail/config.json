{
  "description": "FAIL: Rejection of bundles with invalid mediaType field (not a Sigstore bundle media type)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
