{
  "description": "FAIL: Rejection when unsigned file exists in model directory (without --ignore-unsigned-files)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "model_modifications": {
    "inject": {
      "injected.bin": "injected after signing\n"
    }
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
