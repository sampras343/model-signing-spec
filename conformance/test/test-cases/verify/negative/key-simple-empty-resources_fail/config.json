{
  "description": "FAIL: Rejection of bundle with empty resources array (§5.2.1 — resources MUST contain at least one entry)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
