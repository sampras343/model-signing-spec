{
  "description": "FAIL: Rejection when bundle was signed with key method but verified as certificate method (verification material type mismatch)",
  "method": "certificate",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "cert_chain": [
      "keys/certificate/ca-cert.pem"
    ],
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
