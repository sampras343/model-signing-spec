{
  "description": "FAIL: Tampered file must be detected even when --ignore-unsigned-files is enabled (§8.5 — flag only skips extra-file check, not digest verification)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "model_modifications": {
    "tamper": {
      "signme-1": "TAMPERED CONTENT\n"
    }
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ],
    "ignore_unsigned_files": true
  }
}
