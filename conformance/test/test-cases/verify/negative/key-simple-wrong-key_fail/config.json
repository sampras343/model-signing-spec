{
  "description": "FAIL: Rejection when verification key does not match signing key",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/wrong/wrong-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
