{
  "description": "FAIL: Rejection of bundles with empty signatures array (Section 2.1: MUST have at least one signature)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
