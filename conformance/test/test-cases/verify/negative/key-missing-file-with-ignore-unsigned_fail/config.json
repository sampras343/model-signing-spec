{
  "description": "FAIL: Missing manifest-listed file must be detected even when --ignore-unsigned-files is enabled (§8.5 — flag only skips extra-file check, not presence check)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "model_modifications": {
    "delete": [
      "signme-2"
    ]
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ],
    "ignore_unsigned_files": true
  }
}
