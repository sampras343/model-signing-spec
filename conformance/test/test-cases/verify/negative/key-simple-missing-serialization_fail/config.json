{
  "description": "FAIL: Rejection of bundle with missing serialization object in predicate (§5.2.2 — serialization is REQUIRED)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
