{
  "description": "FAIL: Rejection of bundle with wrong in-toto statement _type (not https://in-toto.io/Statement/v1 per §8.3)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
