{
  "description": "FAIL: Key-signed bundle must be rejected when verified with sigstore method (§4.1 — verification material type mismatch)",
  "method": "sigstore",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "identity": "test@example.com",
    "identity_provider": "https://accounts.example.com",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
