{
  "description": "Verifies files in hidden directories (.cache/, .local/) ARE signed, not excluded",
  "method": "key",
  "model": "models/hidden-subdir",
  "expected_signed_files": [
    ".cache/weights.bin",
    ".local/share/data.bin",
    "model.bin"
  ],
  "sign": {
    "private_key": "keys/certificate/signing-key.pem"
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem"
  }
}
