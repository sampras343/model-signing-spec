{
  "description": "User ignore-paths must match exact relative path only, not nested paths with the same name (§6.2.1)",
  "method": "key",
  "model": "models/with-nested-ignore",
  "expected_signed_files": [
    "signme-1",
    "subdir/ignore-me"
  ],
  "sign": {
    "private_key": "keys/certificate/signing-key.pem"
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}
