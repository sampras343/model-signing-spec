{
  "description": "FAIL: Model containing a symlink must be rejected when allow_symlinks is false (default) (§6.1.1)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "model_modifications": {
    "symlinks": {
      "link-to-signme": "signme-1"
    }
  },
  "sign": {
    "private_key": "keys/certificate/signing-key.pem"
  },
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem"
  }
}
